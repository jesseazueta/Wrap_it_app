<<<<<<< HEAD
<h1>Gifts</h1>

<table>
  <thead>
    <tr>
      <th>Items</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @gifts.each do |gift| %>
      <tr>
        <td><%= gift.name %></td>
        <td><%= link_to 'Show', gift %></td>
        <td><%= link_to 'Edit', edit_gift_path(gift)%></td>
        <td><%= link_to 'Destroy', gift, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
=======
<!-- List all gifts of current user: your own wishlist && wishlist for your contacts/friends -->
<% if params[:category] %>
<% @user.gifts.where(category: params[:category]).each do | gift | %>
<%= gift.name %>
<% end %>
<% else %>
<% @user.gifts.each do |gift| %>
<%= gift.name %>
<% end %> 
<% end %>
>>>>>>> categories
